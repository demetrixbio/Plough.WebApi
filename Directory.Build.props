<Project>
    <PropertyGroup>
        <!-- summary is not migrated from project.json, but you can use the <Description> property for that if needed. -->
        <PackageTags>f#, fsharp</PackageTags>
        <PackageProjectUrl>https://github.com/demetrixbio/Plough.WebApi</PackageProjectUrl>
        <PackageLicenseUrl>https://github.com/demetrixbio/Plough.WebApi/blob/main/LICENSE</PackageLicenseUrl>
        <PackageRequireLicenseAcceptance>false</PackageRequireLicenseAcceptance>
        <RepositoryType>git</RepositoryType>
        <Authors>demetrix</Authors>
        <RepositoryUrl>https://github.com/demetrixbio/Plough.WebApi</RepositoryUrl>
        <!-- owners is not supported in MSBuild -->
    </PropertyGroup>
</Project>